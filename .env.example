# Convex is the registration backend. Integration is still in progress.
VITE_CONVEX_URL=

# Local UI preview only: no email is sent and no registration is persisted.
# Verification codes appear in the browser console. Never enable in production.
VITE_USE_MOCK_API=false
