# Copy to .env.local for local development.
# In production, set these in your host's environment (e.g. Vercel project settings).

# Registration site the "Apply" call to action points at.
# prod: https://register.hackuta.com   dev: http://localhost:5273
VITE_REGISTER_URL=https://register.hackuta.com
