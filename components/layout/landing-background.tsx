/**
 * Full-viewport parchment background. Wave artwork and texture will be added
 * later as separate assets or components.
 */
export function LandingBackground() {
  return (
    <div
      aria-hidden="true"
      className="bg-parchment pointer-events-none fixed inset-0 z-0"
    />
  );
}
